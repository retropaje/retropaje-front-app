import { SxStyles } from "core/types";

export const styles: SxStyles<"root"> = {
  root: {
    display: "flex",
    flexDirection: "column",
    gap: 6,
    padding: 5,
    minWidth: 380,
    minHeight:400,
  },
};
